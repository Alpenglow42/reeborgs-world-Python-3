#not finished

y = 0
x = 0
z = 1

def turn_right():
    #3 lefts make a right
    turn_left()
    turn_left()
    turn_left()
    
def turn_around():
    turn_left()
    turn_left()

def take_object():
    while object_here():
        take()
        #y += 1

        

    
# storm 2    
#think(2)

while z == 1:
    take_object()
    #while object_here():
        #take()
        #y += 1
        #move()
        #x += 1
    if not object_here():
        if front_is_clear():
            move()
            x += 1
        
        elif not front_is_clear():  #erron somewhere below here
            if wall_on right():
                turn_left()
                if front_is_clear():
                    move()
                    take_object()
        elif not front_is_clear():
            turn_right()
            if front_is_clear(): #check for bin corner by seeing if movement possible
                move()
                turn_left()
                if front_is_clear(): #make turn around bin
                    move()
                    if not front_is_clear():
                        turn_around()
                elif not front_is_clear():
                    turn_around()
                
            
        
           
                    
                
                
            
